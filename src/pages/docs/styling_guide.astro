---
import DocsLayout from "../../layouts/DocsLayout.astro";
import CodeBlock from "../../components/CodeBlock.astro";

const headings = [
    {
        depth: 2,
        slug: "tailwind-css-integration",
        text: "Tailwind CSS Integration",
    },
    { depth: 2, slug: "bootstrap-integration", text: "Bootstrap Integration" },
    {
        depth: 2,
        slug: "combining-tailwind-css-and-bootstrap",
        text: "Combining Tailwind CSS and Bootstrap",
    },
    { depth: 2, slug: "custom-css", text: "Custom CSS" },
    { depth: 2, slug: "best-practices", text: "Best Practices" },
];
---

<DocsLayout title="Styling Guide" headings={headings}>
    <h1>Styling Guide</h1>

    <p>
        This guide explains how to style your Snakeskin applications using
        Tailwind CSS and Bootstrap.
    </p>

    <h2 id="tailwind-css-integration">Tailwind CSS Integration</h2>

    <p>
        Snakeskin comes with built-in Tailwind CSS support. Here's how to use
        it:
    </p>

    <h3 id="setup">Setup</h3>

    <p>
        When you create a new project with <code>snakeskin create</code>,
        Tailwind CSS is automatically set up with:
    </p>

    <ol class="list-decimal pl-6 space-y-2 text-neutral-300">
        <li>A <code>tailwind.config.js</code> file for configuration</li>
        <li>
            An <code>input.css</code> file with the basic Tailwind directives
        </li>
        <li>Build process integration to generate the final CSS</li>
    </ol>

    <h3 id="using-tailwind-classes">Using Tailwind Classes</h3>

    <p>
        You can use Tailwind classes directly in your component's render method:
    </p>

    <CodeBlock
        code={`from snakeskin.framework import Component

class TailwindCard(Component):
    def render(self):
        return f"""
        <div class="max-w-md mx-auto bg-white rounded-xl shadow-md overflow-hidden md:max-w-2xl m-4">
            <div class="md:flex">
                <div class="md:shrink-0">
                    <img class="h-48 w-full object-cover md:h-full md:w-48" 
                         src="{self.props.get('image_url', 'https://via.placeholder.com/150')}" 
                         alt="Card image">
                </div>
                <div class="p-8">
                    <div class="uppercase tracking-wide text-sm text-indigo-500 font-semibold">
                        {self.props.get('category', 'Category')}
                    </div>
                    <a href="#" class="block mt-1 text-lg leading-tight font-medium text-black hover:underline">
                        {self.props.get('title', 'Title')}
                    </a>
                    <p class="mt-2 text-gray-500">
                        {self.props.get('description', 'Description')}
                    </p>
                </div>
            </div>
        </div>
        """`}
        language="python"
    />

    <h3 id="customizing-tailwind">Customizing Tailwind</h3>

    <p>
        You can customize Tailwind by editing the <code>tailwind.config.js</code
        > file:
    </p>

    <CodeBlock
        code={`module.exports = {
  content: ["./src/components/**/*.py", "./main.py"],
  theme: {
    extend: {
      colors: {
        'brand-primary': '#3490dc',
        'brand-secondary': '#ffed4a',
        'brand-danger': '#e3342f',
      },
      fontFamily: {
        sans: ['Graphik', 'sans-serif'],
        serif: ['Merriweather', 'serif'],
      },
    },
  },
  plugins: [],
};`}
        language="javascript"
    />

    <h3 id="using-the-tailwindintegration-helper">
        Using the TailwindIntegration Helper
    </h3>

    <p>Snakeskin provides a <code>TailwindIntegration</code> helper class:</p>

    <CodeBlock
        code={`from snakeskin import TailwindIntegration

# Set up Tailwind in a project
TailwindIntegration.setup("./my-project")

# Build Tailwind CSS
TailwindIntegration.build("./dist/styles.css", minify=True)`}
        language="python"
    />

    <h2 id="bootstrap-integration">Bootstrap Integration</h2>

    <p>Snakeskin also supports Bootstrap for styling:</p>

    <h3 id="setup-1">Setup</h3>

    <p>
        To use Bootstrap, you can use the <code>BootstrapIntegration</code> helper:
    </p>

    <CodeBlock
        code={`from snakeskin import BootstrapIntegration

# Set up Bootstrap in a project
BootstrapIntegration.setup("./my-project")

# Get Bootstrap CDN links
links = BootstrapIntegration.get_cdn_links()
print(links['css'])  # CSS link tag
print(links['js'])   # JS script tag

# Include Bootstrap in an HTML template
html_template = """
<!DOCTYPE html>
<html>
<head>
    <title>My App</title>
</head>
<body>
    <div id="app"></div>
</body>
</html>
"""

# Add both CSS and JS
bootstrap_html = BootstrapIntegration.include_in_template(html_template, "both")`}
        language="python"
    />

    <h3 id="using-bootstrap-classes">Using Bootstrap Classes</h3>

    <p>
        You can use Bootstrap classes directly in your component's render
        method:
    </p>

    <CodeBlock
        code={`from snakeskin.framework import Component

class BootstrapCard(Component):
    def render(self):
        return f"""
        <div class="card" style="width: 18rem;">
            <img src="{self.props.get('image_url', 'https://via.placeholder.com/150')}" 
                 class="card-img-top" alt="Card image">
            <div class="card-body">
                <h5 class="card-title">{self.props.get('title', 'Card title')}</h5>
                <p class="card-text">{self.props.get('description', 'Some quick example text.')}</p>
                <a href="#" class="btn btn-primary">Go somewhere</a>
            </div>
        </div>
        """`}
        language="python"
    />

    <h3 id="bootstrap-components">Bootstrap Components</h3>

    <p>
        Bootstrap provides many components that you can use in your Snakeskin
        components:
    </p>

    <CodeBlock
        code={`from snakeskin.framework import Component

class BootstrapNavbar(Component):
    def render(self):
        return """
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Navbar</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
                        data-bs-target="#navbarNav" aria-controls="navbarNav" 
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="#">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Features</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Pricing</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        """`}
        language="python"
    />

    <h2 id="combining-tailwind-css-and-bootstrap">
        Combining Tailwind CSS and Bootstrap
    </h2>

    <p>
        You can use both Tailwind CSS and Bootstrap in the same project, but be
        careful about class name conflicts. Here's how to combine them:
    </p>

    <CodeBlock
        code={`from snakeskin.framework import Component
from snakeskin import BootstrapIntegration

class HybridComponent(Component):
    def render(self):
        return """
        <!-- Bootstrap grid -->
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <!-- Tailwind styled content -->
                    <div class="bg-blue-500 text-white p-4 rounded shadow-lg">
                        <h2 class="text-xl font-bold">Tailwind Section</h2>
                        <p class="mt-2">This section uses Tailwind classes.</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <!-- Bootstrap styled content -->
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Bootstrap Section</h5>
                            <p class="card-text">This section uses Bootstrap classes.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        """

# In main.py
html_content = f"""
<!DOCTYPE html>
<html>
<head>
    <title>Hybrid Styling</title>
    <link href="tailwind.css" rel="stylesheet">
</head>
<body>
    {app.render()}
</body>
</html>
"""

# Add Bootstrap
html_content = BootstrapIntegration.include_in_template(html_content, "both")`}
        language="python"
    />

    <h2 id="custom-css">Custom CSS</h2>

    <p>You can also include custom CSS in your Snakeskin applications:</p>

    <h3 id="inline-styles">Inline Styles</h3>

    <CodeBlock
        code={`def render(self):
    return f"""
    <div style="color: blue; font-size: 18px; margin: 20px;">
        This has inline styles.
    </div>
    """`}
        language="python"
    />

    <h3 id="custom-css-files">Custom CSS Files</h3>

    <ol class="list-decimal pl-6 space-y-2 text-neutral-300">
        <li>Create a CSS file in your project:</li>
    </ol>

    <CodeBlock
        code={`/* src/styles/custom.css */
.custom-component {
    border: 1px solid #ccc;
    padding: 15px;
    border-radius: 5px;
    background-color: #f9f9f9;
}

.custom-component h2 {
    color: #333;
    border-bottom: 1px solid #eee;
    padding-bottom: 10px;
}`}
        language="css"
    />

    <ol start="2" class="list-decimal pl-6 space-y-2 text-neutral-300">
        <li>Include it in your HTML template:</li>
    </ol>

    <CodeBlock
        code={`html_content = f"""
<!DOCTYPE html>
<html>
<head>
    <title>My App</title>
    <link href="tailwind.css" rel="stylesheet">
    <link href="styles/custom.css" rel="stylesheet">
</head>
<body>
    {app.render()}
</body>
</html>
"""`}
        language="python"
    />

    <ol start="3" class="list-decimal pl-6 space-y-2 text-neutral-300">
        <li>Use the custom classes in your components:</li>
    </ol>

    <CodeBlock
        code={`def render(self):
    return """
    <div class="custom-component">
        <h2>Custom Styled Component</h2>
        <p>This uses custom CSS classes.</p>
    </div>
    """`}
        language="python"
    />

    <h2 id="best-practices">Best Practices</h2>

    <ol class="list-decimal pl-6 space-y-2 text-neutral-300">
        <li>
            <strong>Choose one primary styling approach</strong>: Either
            Tailwind CSS or Bootstrap as your main styling system
        </li>
        <li>
            <strong>Use utility classes for one-off styling</strong>: Tailwind
            is great for this
        </li>
        <li>
            <strong>Use component classes for reusable patterns</strong>: Create
            custom CSS for reusable patterns
        </li>
        <li>
            <strong>Be consistent</strong>: Stick to a consistent naming
            convention
        </li>
        <li>
            <strong>Organize styles</strong>: Keep styles organized by component
            or feature
        </li>
        <li>
            <strong>Avoid inline styles</strong>: Use classes whenever possible
            for better maintainability
        </li>
        <li>
            <strong>Consider responsive design</strong>: Use responsive utility
            classes from Tailwind or Bootstrap
        </li>
        <li>
            <strong>Document your styling conventions</strong>: Create a style
            guide for your project
        </li>
    </ol>
</DocsLayout>
